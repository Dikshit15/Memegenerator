import React from 'react';


// Since Header is a component which is only used to display, it should be a functional component

function Header() {
  return <h1>Header</h1>;
}

export default Header;
